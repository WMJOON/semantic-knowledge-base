# canonical_root_hub.yaml
#
# ┌─────────────────────────────────────────────────────────┐
# │  LOCKED — 에이전트/LLM 임의 수정 금지                      │
# │  변경 시 반드시 사용자 명시 지시 + HITL 승인 필요             │
# └─────────────────────────────────────────────────────────┘
version: "1.1"
locked: true

domains: []

scan:
  include: "ontology/system/semantic/**/*.ttl"
  exclude:
    - "ontology/system/semantic/**/*.shapes.ttl"
    - "ontology/system/semantic/**/*.inferred.ttl"
  orphan_exit_code: 1

sync:
  structural_ssot: ttl
  projection_target: md
  auto_apply_md_to_ttl: false
