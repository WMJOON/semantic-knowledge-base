x_msm_generated:
  skill: skb-repository-setup
  version: "1.0.0"

# L1 fixture stub — `skb-harness-SPEC`에서 본격적 fixture 정의.
fixture:
  name: minimal_init_plan
  description: |
    빈 target 디렉토리에 대해 plan_init.py가 모든 entry를 'create'로 분류해야 한다.
  inputs:
    target: ":scratch:/minimal"
    options:
      targets: "claude,codex"
  expected:
    plan:
      creates_min: 50
      conflicts: 0
      hitl_requests: 0
